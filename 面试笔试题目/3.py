numbers = [1, 2, 4, 5, 6]
# n
numbers.sort()


def get_smallest_index(start, end, x):
    middle = int((start+end)/2)
    if numbers[middle] <= x:

    if numbers[middle] > x:
        get_smallest_index
